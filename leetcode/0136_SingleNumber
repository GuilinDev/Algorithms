class Solution {
    public int singleNumber(int[] nums) {
        int sn = 0;
        
        for (int i = 0; i < nums.length; i++) {
            sn ^= nums[i]; // logical exclusive OR
        }
        
        return sn;
    }
}
